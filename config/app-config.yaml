app:
  aws:
    dynamo:
      region: local
      endpoint: http://10.11.0.3:8000
      tables:
        users: users
      httpClient:
        connectionTimeout: 10s

  server:
    port: :8091
    shutdownTimeout: 20s