app:
  aws:
    dynamo:
      region: local
      endpoint: http://localhost:8042
      tables:
        users: users
      httpClient:
        connectionTimeout: 10s

  server:
    port: :8091
    shutdownTimeout: 20s